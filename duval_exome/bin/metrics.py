#!/usr/bin/env python
# encoding: utf-8
"""
parses metrics and stats files into a single table
"""

from argparse import ArgumentParser, ArgumentDefaultsHelpFormatter

def main():

if __name__ == '__main__':
    p = ArgumentParser(description=__doc__, formatter_class=)
    p.add_argument("-aln", nargs="+", help="alignment stats file")
    args = p.parse_args()
    main(args.)
